package webview

func ExampleWithDebugURL() {
	symbolName := "WithDebugURL"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWithTimeout() {
	symbolName := "WithTimeout"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWithConsoleLimit() {
	symbolName := "WithConsoleLimit"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleNew() {
	symbolName := "New"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_Close() {
	symbolName := "Webview Close"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_Navigate() {
	symbolName := "Webview Navigate"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_Click() {
	symbolName := "Webview Click"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_Type() {
	symbolName := "Webview Type"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_QuerySelector() {
	symbolName := "Webview QuerySelector"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_QuerySelectorAll() {
	symbolName := "Webview QuerySelectorAll"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_QuerySelectorAllAll() {
	symbolName := "Webview QuerySelectorAllAll"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_GetConsole() {
	symbolName := "Webview GetConsole"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_GetConsoleAll() {
	symbolName := "Webview GetConsoleAll"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_ClearConsole() {
	symbolName := "Webview ClearConsole"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_Screenshot() {
	symbolName := "Webview Screenshot"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_Evaluate() {
	symbolName := "Webview Evaluate"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_WaitForSelector() {
	symbolName := "Webview WaitForSelector"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_GetURL() {
	symbolName := "Webview GetURL"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_GetTitle() {
	symbolName := "Webview GetTitle"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_GetHTML() {
	symbolName := "Webview GetHTML"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_SetViewport() {
	symbolName := "Webview SetViewport"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_SetUserAgent() {
	symbolName := "Webview SetUserAgent"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_Reload() {
	symbolName := "Webview Reload"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_GoBack() {
	symbolName := "Webview GoBack"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}

func ExampleWebview_GoForward() {
	symbolName := "Webview GoForward"
	if len(symbolName) == 0 {
		panic("missing example symbol")
	}
}
